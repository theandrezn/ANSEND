---
phase: 01-auditoria-da-implementacao-atual
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-auditoria-da-implementacao-atual/01-FRONTEND-AUDIT.md
autonomous: true
requirements: [AUD-01, AUD-04]
---

<objective>
Consolidar a auditoria da rota real `#compras`, seu renderer, estado, queries, navegação, estilos, ações e integrações compartilhadas sem alterar código funcional.

Purpose: estabelecer a superfície canônica que as fases seguintes devem preservar e identificar dados locais/fallbacks que não podem continuar como fonte final.
Output: `.planning/phases/01-auditoria-da-implementacao-atual/01-FRONTEND-AUDIT.md`.
</objective>

<must_haves>
- A rota `#compras` e seus pontos de entrada estão documentados com caminhos e símbolos reais.
- Listagem, detalhe, estado, carregamento, filtros e ações estão rastreados.
- Perfil, chat, player, autenticação e componentes globais compartilhados estão identificados.
- Cada elemento auditado recebe classificação preservar, reutilizar, corrigir ou remover.
- `localStorage`, fallbacks, dados estáticos e estilos inline são explicitamente avaliados.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Inventariar rota, renderer, estado e dados do frontend</name>
  <read_first>
    <file>index.html</file>
    <file>script.js</file>
    <file>styles.css</file>
    <file>.planning/codebase/ARCHITECTURE.md</file>
    <file>.planning/REQUIREMENTS.md</file>
  </read_first>
  <action>Rastrear a navegação `#compras`, `protectedRoute()`, `renderRoute()`, `renderPurchases()`, parâmetros do hash, `loadUserPurchases()`, consultas Supabase, estado `appState`, `persistState()`, filtros, paginação, contratos e downloads. Registrar linhas/símbolos e diferenciar banco, cache, fallback e estado temporário.</action>
  <acceptance_criteria>
    <criterion>O documento contém seções Route and Entry Points, State and Data Sources, List Flow e Detail Flow.</criterion>
    <criterion>O documento menciona `#compras`, `renderPurchases`, `loadUserPurchases`, `appState.purchases`, `ansend-purchases` e `downloadPurchasedFile`.</criterion>
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Mapear integrações compartilhadas e matriz frontend</name>
  <read_first>
    <file>script.js</file>
    <file>index.html</file>
    <file>styles.css</file>
    <file>tests/route-stability-check.js</file>
    <file>tests/licensing-system-check.js</file>
  </read_first>
  <action>Documentar como a área reutiliza autenticação, perfil público, chat direto, player, shell/sidebar, toasts, empty states e Lucide. Classificar cada componente/fluxo como preservar, reutilizar, corrigir ou remover, incluindo impactos e testes existentes.</action>
  <acceptance_criteria>
    <criterion>O documento contém Shared Components and Flows, Existing Tests, Risks e Frontend Change Matrix.</criterion>
    <criterion>A matriz inclui rota/renderer, queries, perfil, chat, player, `localStorage`, paginação frontend, contrato fallback e estilos inline.</criterion>
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Confirmar que somente arquivos em `.planning/` foram alterados.
- Confirmar que todos os símbolos citados existem no código atual.
- Confirmar cobertura de AUD-01 e da parcela frontend de AUD-04.
</verification>

<success_criteria>
- Auditoria frontend substantiva e acionável criada.
- Nenhum código, estilo, SQL, Worker, teste ou `dist/` alterado.
- Superfície existente e compartilhada claramente delimitada para fases futuras.
</success_criteria>
