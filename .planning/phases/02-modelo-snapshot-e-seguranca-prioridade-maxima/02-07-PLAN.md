---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - supabase/tests/purchase_lifecycle.sql
  - tests/run-purchase-db-tests.mjs
  - tests/purchase-webhook-delivery-replay-check.mjs
  - tests/purchase-security-contract-check.js
  - package.json
  - package-lock.json
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05, DATA-06, SEC-01, SEC-02, SEC-03, SEC-04, PAY-02, PAY-06]
must_haves:
  truths:
    - "Todos os cenários obrigatórios passam em banco isolado e checks existentes não regrediram."
    - "Teste de banco não pode ser pulado nem apontar para produção."
    - "A Fase 3 permanece bloqueada até backfill e critérios de aceite completos."
  artifacts:
    - path: "supabase/tests/purchase_lifecycle.sql"
      provides: "Suíte integrada de lifecycle/RLS/backfill"
    - path: "tests/run-purchase-db-tests.mjs"
      provides: "Runner seguro de banco isolado"
    - path: "tests/purchase-webhook-delivery-replay-check.mjs"
      provides: "Replay Worker"
  key_links:
    - from: "package.json test:purchases:phase2"
      to: "SQL + Worker + security + existing regressions"
      via: "gate agregado"
---

<objective>
Provar a Fase 2 ponta a ponta e impedir avanço visual enquanto qualquer garantia estiver faltando.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-06-SUMMARY.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md
</context>

<required_test_matrix>
1. Pedido pago com order_items gera direitos corretos.
2. Pedido sem order_items não gera entitlement.
3. Replay do mesmo webhook não duplica registros.
4. Webhooks concorrentes não duplicam registros.
5. Pagamento pendente não libera arquivos.
6. Pagamento falho não gera direitos.
7. `completed -> completed` não é necessário.
8. Falha intermediária não deixa estado parcial.
9. Usuário A não acessa pedido do usuário B.
10. Produtor acessa apenas vendas próprias.
11. Alteração futura na licença não altera snapshot histórico.
12. Backfill executado duas vezes não duplica registros.
13. Constraints impedem duplicação diretamente no banco.
14. Build e testes existentes continuam funcionando.
</required_test_matrix>

<threat_model>
- **TM-19, Critical, False assurance:** checks textuais passam sem DB. Mitigação: integração SQL obrigatória.
- **TM-20, Critical, Production test:** fixtures atingem produção. Mitigação: runner recusa project ref de produção.
- **TM-21, High, Regression:** checkout/download existente quebra. Mitigação: suíte existente + build.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Criar suíte integrada de lifecycle, falhas e snapshot</name>
  <current_behavior>1. Testes atuais não executam a transação completa em Postgres.</current_behavior>
  <evidence>2. Checks existentes leem arquivos; webhook replay testa timestamp, não delivery cardinality.</evidence>
  <action>3. Criar SQL/runner cobrindo: paid+items gera direitos; sem items não gera; pending/failed não libera; replay sequencial/concorrente; completed->completed desnecessário; falha injetada sem parcial; snapshot imutável; constraint direta; backfill duas vezes no-op.</action>
  <files>4. `supabase/tests/purchase_lifecycle.sql`, `tests/run-purchase-db-tests.mjs`, package files.</files>
  <db_objects>5. Todos os objetos de lifecycle dos Planos 2-6.</db_objects>
  <dependencies>6. Implementações e migrations validadas em banco isolado.</dependencies>
  <data_risk>7. Alto se runner apontar para produção.</data_risk>
  <rollback>8. Cada teste usa transação/fixtures com cleanup; runner bloqueia ref `qxujynzqdursxaehchik`.</rollback>
  <verify>9. `npm run test:purchases:db` executa todos os cenários e falha se DB não estiver disponível.</verify>
  <done>10. Os cenários de lifecycle desta tarefa passam contra Postgres isolado.</done>
</task>

<task type="auto">
  <name>Task 2: Testar webhook, concorrência, RLS e downloads</name>
  <current_behavior>1. HMAC/topic/timestamp e boundaries têm checks separados, sem ligação completa ao pedido/delivery.</current_behavior>
  <evidence>2. Worker converge em reconcile/finalize; RLS atual possui policies legadas e download buyer+beat.</evidence>
  <action>3. Testar mesmo webhook/status/checkout repetidos, duas conexões concorrentes, buyer A/B, producer próprio/alheio, external, service role, pending/failed/refunded download e completed+active. Combinar mocks do Worker com cardinalidade real do RPC.</action>
  <files>4. `tests/purchase-webhook-delivery-replay-check.mjs`, `tests/purchase-security-contract-check.js`, SQL lifecycle.</files>
  <db_objects>5. RLS/grants, finalizer, delivery, storage/download authorization.</db_objects>
  <dependencies>6. Task 1 e Planos 3/5.</dependencies>
  <data_risk>7. Baixo em ambiente isolado; risco de segredo em logs.</data_risk>
  <rollback>8. Mocks/fixtures efêmeros e logs sanitizados.</rollback>
  <verify>9. Executar comandos focados e confirmar uma venda exclusiva, um pedido e um delivery sob concorrência.</verify>
  <done>10. Replay, isolamento, privacidade e download server-side comprovados.</done>
</task>

<task type="auto">
  <name>Task 3: Executar regressões, build e gate de aceite</name>
  <current_behavior>1. Carrinho, checkout, auth, perfil, chat, player e catálogo já funcionam e não podem regredir.</current_behavior>
  <evidence>2. ROADMAP e change matrix exigem preservação; produção serve build e Worker atuais.</evidence>
  <action>3. Criar `test:purchases:phase2` agregando DB/Worker/security e executar checkout database, Mercado Pago, licensing, data boundaries, routes e build. Não fazer deploy. Revalidar git diff e confirmar ausência de alterações visuais/#compras.</action>
  <files>4. `package.json`, `package-lock.json`; nenhum arquivo visual.</files>
  <db_objects>5. Nenhuma alteração; somente verificação.</db_objects>
  <dependencies>6. Tasks 1-2 e relatório backfill com zero ambíguos para aceite final.</dependencies>
  <data_risk>7. Build pode atualizar dist; revisar e não deployar.</data_risk>
  <rollback>8. Descartar somente artefato gerado não aprovado; não reverter trabalho alheio.</rollback>
  <verify>9. Suite agregada, testes existentes, `npm run build`, `git diff --check` e inspeção completa do diff.</verify>
  <done>10. Build/testes passam, nenhum fluxo visual mudou e todos os critérios da fase têm evidência.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Aprovar conclusão da Fase 2</name>
  <current_behavior>1. Código/migrations estão testados; nenhum deploy é automático.</current_behavior>
  <evidence>2. Relatórios, suíte, build e diff final disponíveis.</evidence>
  <action>3. Apresentar evidências, riscos residuais e pedido explícito de aprovação antes de produção/Fase 3.</action>
  <files>4. Nenhum.</files>
  <db_objects>5. Nenhum.</db_objects>
  <dependencies>6. Tasks 1-3 e zero ambíguos.</dependencies>
  <data_risk>7. Nenhum sem aprovação.</data_risk>
  <rollback>8. Manter Fase 3 bloqueada.</rollback>
  <verify>9. Aprovação explícita do usuário.</verify>
  <done>10. Fase aprovada ou formalmente permanece bloqueada.</done>
</task>

</tasks>

<verification>
- [ ] Todos os 14 cenários solicitados possuem teste.
- [ ] Constraints testadas diretamente no banco.
- [ ] Build e regressões existentes passam.
- [ ] Banco isolado obrigatório; produção recusada pelo runner.
- [ ] Nenhum deploy ou avanço para Fase 3.
</verification>

<success_criteria>
- Requisitos 12/12 com evidência executável.
- Zero estado parcial, duplicidade ou vazamento cross-user.
- Backfill no-op em rerun e zero ambíguos para conclusão.
- Aprovação humana antes de produção ou Fase 3.
</success_criteria>

<output>After completion, create `02-07-SUMMARY.md`.</output>
