---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - supabase/migrations/20260622230000_purchase_backfill.sql
  - supabase/schema.sql
  - supabase/tests/purchase_backfill.sql
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md
autonomous: false
requirements: [DATA-02, DATA-04, SEC-03, SEC-04]
must_haves:
  truths:
    - "Backfill separa corrigível de ambíguo e nunca inventa direito."
    - "Dry-run precede qualquer escrita e rerun é no-op."
    - "Caso ambíguo bloqueia conclusão da Fase 2."
  artifacts:
    - path: "supabase/migrations/20260622230000_purchase_backfill.sql"
      provides: "Função auditável dry-run/apply"
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md"
      provides: "Contagens e decisões sem PII"
  key_links:
    - from: "audit_purchase_delivery_gaps"
      to: "provision_purchase_delivery"
      via: "somente casos comprováveis"
---

<objective>
Auditar e reparar pedidos históricos de forma idempotente, com dry-run, registro e bloqueio de ambiguidades.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-05-SUMMARY.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DATA-AUDIT.md
</context>

<threat_model>
- **TM-16, Critical, Overgrant:** backfill inventa direitos superiores. Mitigação: hierarquia de evidência e ambiguous stop.
- **TM-17, High, Duplicate:** rerun duplica delivery. Mitigação: delivery_key unique + upsert.
- **TM-18, High, Disclosure:** audit log vaza PII. Mitigação: IDs técnicos, classificação e razão sem dados pessoais.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implementar classificador e dry-run persistente</name>
  <current_behavior>1. Não existe backfill; pedidos pagos antigos podem não ter delivery e snapshot completo.</current_behavior>
  <evidence>2. Plano 1 mede gaps; discussão exige A corrigíveis e B ambíguos, com ambíguos bloqueando a fase.</evidence>
  <action>3. Criar `audit_purchase_delivery_gaps(p_apply boolean default false, p_run_id uuid)` service-role only. Classificar analyzed/corrected/skipped/ambiguous usando snapshots, payment attempt ligado e fontes atuais apenas quando timestamps provarem que não mudaram. Se o Plano 1 provar ausência de estrutura de auditoria adequada, criar uma única tabela mínima `purchase_backfill_audit` sem PII; caso contrário reutilizar estrutura existente.</action>
  <files>4. Migration 20260622230000, `schema.sql`, teste SQL.</files>
  <db_objects>5. Função de auditoria; opcional `purchase_backfill_audit` justificada; tabelas comerciais somente leitura em dry-run.</db_objects>
  <dependencies>6. Diagnóstico Plano 1, constraints, finalização, snapshot e RLS dos Planos 2-5.</dependencies>
  <data_risk>7. Crítico no modo apply; zero escrita comercial no dry-run.</data_risk>
  <rollback>8. Revogar/drop da função e tabela de auditoria vazia; não apagar correções comerciais sem análise individual.</rollback>
  <verify>9. Dry-run em fixtures retorna quatro classificações e zero alterações em orders/items/delivery.</verify>
  <done>10. Todo pedido analisado recebe classificação e razão auditável.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar apply idempotente somente para casos A</name>
  <current_behavior>1. Não há reparo seguro; usar catálogo atual sem prova poderia ampliar licença.</current_behavior>
  <evidence>2. Contexto D-05 a D-09 proíbe inventar termos e exige rerun no-op.</evidence>
  <action>3. No p_apply=true, processar somente corrigíveis, preencher snapshot comprovado, criar delivery/contrato pelas funções canônicas e registrar corrected. Registrar skipped íntegros e ambiguous sem escrever direitos. Nunca deletar ou sobrescrever documento divergente; elevar para auditoria manual.</action>
  <files>4. Migration 20260622230000, `schema.sql`, `supabase/tests/purchase_backfill.sql`.</files>
  <db_objects>5. `order_items`, entitlements, documents, ledger e audit function/table.</db_objects>
  <dependencies>6. Task 1 e delivery_key unique.</dependencies>
  <data_risk>7. Crítico: concessão jurídica incorreta; apply deve abortar transação em qualquer divergência não classificada.</data_risk>
  <rollback>8. Run_id identifica writes; rollback individual só com evidência e aprovação, preferindo revogar em vez de apagar.</rollback>
  <verify>9. Executar apply duas vezes em fixtures; segunda execução tem zero inserts e mesmas contagens.</verify>
  <done>10. Corrigíveis ficam íntegros exatamente uma vez; ambíguos permanecem sem direito inventado.</done>
</task>

<task type="auto">
  <name>Task 3: Produzir dry-run real e relatório para aprovação</name>
  <current_behavior>1. Quantidade real de casos A/B só é conhecida após diagnóstico no alvo.</current_behavior>
  <evidence>2. Nenhuma aplicação em produção foi autorizada; usuário exige parada para revisão.</evidence>
  <action>3. Executar apenas p_apply=false no alvo autorizado, registrar timestamp/ambiente/contagens analyzed/correctable/skipped/ambiguous e IDs técnicos mínimos em `02-BACKFILL-REPORT.md`. Não executar apply nem migrations em produção nesta etapa de planejamento/execução sem aprovação separada.</action>
  <files>4. `02-BACKFILL-REPORT.md`.</files>
  <db_objects>5. Função dry-run somente leitura; nenhuma tabela comercial modificada.</db_objects>
  <dependencies>6. Tasks 1-2 validadas em banco isolado e autorização de leitura do alvo.</dependencies>
  <data_risk>7. Baixo para dados; risco de PII no relatório.</data_risk>
  <rollback>8. Remover relatório inseguro e regenerar sanitizado; nenhum rollback de banco.</rollback>
  <verify>9. Comparar contagens ao Plano 1 e confirmar zero DML pela auditoria/transação.</verify>
  <done>10. Relatório contém todos os status exigidos e decisão pendente de apply.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Aprovar backfill real e resolver ambíguos</name>
  <current_behavior>1. Somente dry-run foi executado.</current_behavior>
  <evidence>2. `02-BACKFILL-REPORT.md` mostra casos A e B.</evidence>
  <action>3. Solicitar aprovação do apply; cada B exige evidência manual ou mantém a fase bloqueada.</action>
  <files>4. Nenhum.</files>
  <db_objects>5. Nenhum até aprovação.</db_objects>
  <dependencies>6. Task 3.</dependencies>
  <data_risk>7. Crítico após aprovação; nenhum antes.</data_risk>
  <rollback>8. Não prosseguir é o rollback seguro padrão.</rollback>
  <verify>9. Aprovação explícita e ambiguous_count=0.</verify>
  <done>10. Apply autorizado com zero ambíguos ou fase formalmente parada.</done>
</task>

</tasks>

<verification>
- [ ] Dry-run não escreve.
- [ ] Apply é idempotente em teste.
- [ ] Todos os analisados têm status.
- [ ] Ambíguos não recebem direitos.
- [ ] Nenhum apply real sem aprovação.
</verification>

<success_criteria>
- Backfill repetível, auditável e conservador.
- Zero duplicação e zero overgrant.
- Fase bloqueada enquanto houver ambiguidade.
</success_criteria>

<output>After completion, create `02-06-SUMMARY.md`.</output>
