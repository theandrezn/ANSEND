---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - supabase/migrations/20260622220000_immutable_purchase_snapshot.sql
  - supabase/schema.sql
  - tests/purchase-snapshot-contract-check.js
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - "Compra concluída preserva todos os dados comerciais e jurídicos vigentes."
    - "Editar ou remover beat/licença/perfil não reescreve o histórico."
    - "Perfil público atual permanece apenas como vínculo dinâmico separado."
  artifacts:
    - path: "supabase/migrations/20260622220000_immutable_purchase_snapshot.sql"
      provides: "Snapshot estruturado e preenchimento transacional"
    - path: "tests/purchase-snapshot-contract-check.js"
      provides: "Contrato de imutabilidade"
  key_links:
    - from: "beat_licenses"
      to: "order_items.license_rights_snapshot"
      via: "captura no checkout sob lock"
---

<objective>
Persistir snapshot imutável, suficiente para compra, contrato e direito sem consultar o catálogo futuro.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-03-SUMMARY.md
@supabase/schema.sql
</context>

<threat_model>
- **TM-10, High, Tampering:** edição futura altera direitos históricos. Mitigação: JSON snapshot imutável preenchido uma vez.
- **TM-11, High, Information disclosure:** identidade contratual exposta ao produtor em colunas. Mitigação: identidade no order/documento autorizado, não em projection ampla.
- **TM-12, High, Data loss:** FK cascade apaga identidade histórica. Mitigação: snapshots text/UUID sem cascade destrutiva.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Definir e adicionar o snapshot mínimo completo</name>
  <current_behavior>1. `order_items` guarda nome/termos/preço/royalties/files em texto, mas não currency, produtor/beat histórico nem direitos completos.</current_behavior>
  <evidence>2. `beat_licenses` possui stream/video limits, usos, Content ID, crédito, duração e território que não aparecem no item.</evidence>
  <action>3. Adicionar de forma aditiva: beat title/cover/id histórico; producer id/name histórico; license id/key/name; price/currency; `license_rights_snapshot jsonb`; `file_manifest_snapshot jsonb`; termos/royalties/aceite/version; `buyer_identity_snapshot jsonb` no order somente com campos contratuais realmente fornecidos. Proibir provider metadata, secrets e URLs assinadas.</action>
  <files>4. Migration 20260622220000, `schema.sql`, teste de snapshot.</files>
  <db_objects>5. `orders`, `order_items`; constraints JSON/checks e índices necessários.</db_objects>
  <dependencies>6. Ordem transacional do Plano 3.</dependencies>
  <data_risk>7. Médio: colunas NOT NULL quebram legado; começar nullable/NOT VALID e validar após Plano 6.</data_risk>
  <rollback>8. Manter colunas aditivas; reverter apenas writes da função se necessário, sem apagar snapshots já capturados.</rollback>
  <verify>9. Teste compara todos os campos relevantes de beat_licenses com o JSON esperado e rejeita chaves proibidas.</verify>
  <done>10. Snapshot cobre beat, produtor, licença, preço, moeda, direitos, restrições, royalties, formatos e contrato.</done>
</task>

<task type="auto">
  <name>Task 2: Capturar snapshot sob os mesmos locks da compra</name>
  <current_behavior>1. `process_checkout` relê licença durante o loop e monta snapshot parcial; contrato atual consulta beat/profile vivos.</current_behavior>
  <evidence>2. `manage_purchase_entitlements` faz join com beats/profiles e usa stream limit hardcoded `Ilimitados`.</evidence>
  <action>3. Ajustar as funções do Plano 3 para selecionar todos os campos uma vez sob lock e inserir snapshot junto com each order_item. `provision_purchase_delivery` e gerador de contrato devem ler somente snapshot; perfil atual pode ser resolvido separadamente pela futura UI, sem sobrescrever histórico.</action>
  <files>4. Migration 20260622220000, `schema.sql`.</files>
  <db_objects>5. `process_checkout`, `provision_purchase_delivery`, `generate_contract_text_sql`, `order_items`, `license_documents`.</db_objects>
  <dependencies>6. Task 1 e helpers do Plano 3.</dependencies>
  <data_risk>7. Alto se contrato e entitlement usarem fontes diferentes.</data_risk>
  <rollback>8. Reverter versão das funções mantendo novas colunas; pausar checkout se snapshot não puder ser validado.</rollback>
  <verify>9. Criar compra, alterar beat/licença/perfil e confirmar item, entitlement e contrato byte-equivalentes ao snapshot original.</verify>
  <done>10. Toda entrega deriva do snapshot e alterações futuras não afetam compra antiga.</done>
</task>

<task type="auto">
  <name>Task 3: Validar identidade histórica e vínculo dinâmico</name>
  <current_behavior>1. Buyer name/email ficam em orders; produtor e beat históricos dependem de relações atuais.</current_behavior>
  <evidence>2. Discussão aprovou identidade contratual congelada e perfil atual apenas como vínculo separado.</evidence>
  <action>3. Definir projection/RLS de forma que contrato use buyer/produtor históricos; producer não recebe buyer_identity_snapshot como coluna ampla. Manter producer_id_snapshot para link ao perfil atual quando existir, com fallback histórico se removido.</action>
  <files>4. Migration 20260622220000, `schema.sql`, teste.</files>
  <db_objects>5. Colunas snapshot e `license_documents`.</db_objects>
  <dependencies>6. Tasks 1-2 e Plano 5 para enforcement final.</dependencies>
  <data_risk>7. Privacidade: e-mail/documento pode vazar em selects do produtor.</data_risk>
  <rollback>8. Remover projection/grant indevido imediatamente; preservar documento autorizado já gerado.</rollback>
  <verify>9. Teste de schema confirma separação entre snapshot comercial do item e identidade privada no order/documento.</verify>
  <done>10. Histórico permanece íntegro e perfil atual é somente referência opcional.</done>
</task>

</tasks>

<verification>
- [ ] Snapshot completo e estruturado.
- [ ] Contrato e entitlement usam somente snapshot.
- [ ] Alteração futura não muda compra antiga.
- [ ] Nenhuma PII/provider secret indevida no item.
</verification>

<success_criteria>
- DATA-01 e DATA-02 comprováveis.
- Snapshot é suficiente para backfill e entrega.
- Identidade histórica e perfil atual não se confundem.
</success_criteria>

<output>After completion, create `02-04-SUMMARY.md`.</output>
