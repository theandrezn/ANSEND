---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - supabase/migrations/20260622223000_purchase_rls_authorization.sql
  - supabase/schema.sql
  - src/worker.mjs
  - tests/purchase-security-contract-check.js
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]
must_haves:
  truths:
    - "Comprador lê apenas a própria compra e direitos."
    - "Produtor lê somente vendas próprias e dados mínimos."
    - "Escritas privilegiadas e downloads permanecem server-side com RLS ativa."
  artifacts:
    - path: "supabase/migrations/20260622223000_purchase_rls_authorization.sql"
      provides: "RLS, grants e policies mínimas"
    - path: "tests/purchase-security-contract-check.js"
      provides: "Contrato de autorização"
  key_links:
    - from: "src/worker.mjs /api/orders/download"
      to: "purchase_entitlements"
      via: "buyer + active + completed + allowed format"
---

<objective>
Fechar policies e grants legados e garantir autorização mínima para compras, contratos e downloads.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-04-SUMMARY.md
@supabase/schema.sql
@src/worker.mjs
</context>

<threat_model>
- **TM-13, Critical, Elevation:** authenticated fabrica order/item completed. Mitigação: remover insert policies/grants.
- **TM-14, High, Disclosure:** buyer/produtor acessa terceiros. Mitigação: RLS por buyer_id/producer snapshot e testes com claims distintos.
- **TM-15, Critical, Download bypass:** storage libera arquivo sem entitlement. Mitigação: backend signed URL + completed/active/formato/path.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Remover escrita direta e restringir funções privilegiadas</name>
  <current_behavior>1. Policies legadas permitem INSERT authenticated em orders/order_items, embora finalização deva ser server-side.</current_behavior>
  <evidence>2. `Users can insert their own orders/items` permanece no schema; finalizer está concedido apenas a service_role.</evidence>
  <action>3. Dropar policies de insert direto; revogar INSERT/UPDATE/DELETE de anon/authenticated em orders/items/entitlements/documents/ledger. Revogar execute de helpers internos; manter apenas service role no finalizador/backfill. Fixar search_path e ownership das security definer.</action>
  <files>4. Migration 20260622223000, `schema.sql`, teste de segurança.</files>
  <db_objects>5. Policies/grants das tabelas de compra e funções privilegiadas.</db_objects>
  <dependencies>6. Funções finais dos Planos 3-4.</dependencies>
  <data_risk>7. Médio: cliente legado poderia depender de insert direto; confirmar Worker atual antes de revogar.</data_risk>
  <rollback>8. Restaurar policy mínima somente sob incidente comprovado, nunca grant amplo; preferir corrigir Worker.</rollback>
  <verify>9. Como authenticated, tentativas de insert/update/delete e execute falham; service role continua operando.</verify>
  <done>10. Nenhum cliente fabrica pedido, item, direito ou contrato.</done>
</task>

<task type="auto">
  <name>Task 2: Minimizar leitura de comprador e produtor</name>
  <current_behavior>1. Buyer policies existem; producer lê order_items via beat atual e entitlements via beats, o que pode quebrar após exclusão e expor mais que o necessário.</current_behavior>
  <evidence>2. CONTEXT D-10 a D-13 limita produtor ao contrato autorizado e dados mínimos das próprias vendas.</evidence>
  <action>3. Buyer select por buyer_id/order ownership. Producer select baseado em producer_id_snapshot/producer_id da venda, apenas order item comercial necessário, documento e ledger próprios; remover acesso amplo a entitlement se desnecessário. Não expor buyer identity/payment metadata em projection do produtor.</action>
  <files>4. Migration 20260622223000, `schema.sql`.</files>
  <db_objects>5. RLS de orders, items, attempts, entitlements, documents, logs, ledger.</db_objects>
  <dependencies>6. Snapshot do Plano 4.</dependencies>
  <data_risk>7. Alto para privacidade e suporte do produtor se policy for ampla ou estreita demais.</data_risk>
  <rollback>8. Reverter policy específica por nome; RLS nunca é desativada.</rollback>
  <verify>9. Matriz buyer A/buyer B/producer A/producer B/external confirma apenas acessos autorizados.</verify>
  <done>10. Isolamento e acesso mínimo comprovados sem depender de UI.</done>
</task>

<task type="auto">
  <name>Task 3: Endurecer autorização de download sem trocar o sistema assinado</name>
  <current_behavior>1. Worker autentica e procura active entitlement por buyer+beat, valida formato/path e assina por 300s; storage policy também aceita completed order.</current_behavior>
  <evidence>2. Downloads assinados devem ser preservados, mas pedido pending/falho ou sem entitlement nunca pode liberar arquivo.</evidence>
  <action>3. Manter endpoint e signed URL. Exigir simultaneamente buyer correto, order completed, entitlement active, order_item correspondente e formato no snapshot; negar pending/failed/refunded. Remover qualquer policy direta de storage que permita buyer baixar apenas por completed order sem entitlement, mantendo upload/owner e serviço de assinatura.</action>
  <files>4. Migration 20260622223000, `schema.sql`, `src/worker.mjs`, teste.</files>
  <db_objects>5. Storage policies, `purchase_entitlements`, `orders`, `/api/orders/download`.</db_objects>
  <dependencies>6. Tasks 1-2 e snapshot/delivery estáveis.</dependencies>
  <data_risk>7. Alto: policy errada pode bloquear compradores válidos ou liberar terceiros.</data_risk>
  <rollback>8. Restaurar última policy segura e manter endpoint fechado; nunca tornar bucket público.</rollback>
  <verify>9. Testes pending/failed/refunded/foreign/wrong-format negam; completed+active+allowed assina URL curta.</verify>
  <done>10. Arquivo só é liberado pelo backend para o direito histórico correto.</done>
</task>

</tasks>

<verification>
- [ ] RLS permanece ativa.
- [ ] Nenhuma service key no frontend.
- [ ] Escrita direta authenticated negada.
- [ ] Buyer/produtor/externo isolados.
- [ ] Signed downloads preservados e endurecidos.
</verification>

<success_criteria>
- SEC-01 a SEC-04 comprováveis no banco e Worker.
- Sem rota ou UI nova.
- Privilégios mínimos e reversíveis por policy.
</success_criteria>

<output>After completion, create `02-05-SUMMARY.md`.</output>
