---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-TRANSACTION-CONTRACT.md
  - supabase/migrations/20260622210000_purchase_snapshot_foundation.sql
  - supabase/schema.sql
  - tests/purchase-snapshot-contract-check.js
  - package.json
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, SEC-03]
user_setup: []
must_haves:
  truths:
    - "Cada item novo preserva beat, produtor, comprador contratual, licença, preço, moeda, arquivos, direitos e restrições sem depender do catálogo depois da compra."
    - "A identidade histórica da compra e o vínculo opcional ao perfil atual permanecem conceitos separados."
    - "As responsabilidades de Worker, finalizador, checkout, provisionamento e trigger ficam explícitas antes da correção transacional."
  artifacts:
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-TRANSACTION-CONTRACT.md"
      provides: "Contrato da ordem transacional e das responsabilidades existentes"
      contains: "finalize_checkout_payment"
    - path: "supabase/migrations/20260622210000_purchase_snapshot_foundation.sql"
      provides: "Campos estruturados de snapshot e desenho das chaves por item"
      contains: "license_rights_snapshot"
    - path: "tests/purchase-snapshot-contract-check.js"
      provides: "Regressão estrutural do snapshot no schema e migration"
  key_links:
    - from: "supabase/migrations/20260622210000_purchase_snapshot_foundation.sql"
      to: "supabase/schema.sql"
      via: "mesmas colunas, constraints e índices no schema consolidado"
      pattern: "license_rights_snapshot"
    - from: "tests/purchase-snapshot-contract-check.js"
      to: "supabase/schema.sql"
      via: "assertions sobre o snapshot histórico completo"
      pattern: "buyer_identity_snapshot"
---

<objective>
Definir o contrato transacional alvo e criar a fundação de snapshot imutável e unicidade por item sem ainda trocar a ordem de finalização.

Purpose: remover ambiguidades antes da alteração crítica e garantir que a compra possua dados suficientes para direitos, contrato, backfill e futura UI sem consultar valores atuais do catálogo.
Output: contrato técnico da transação, migration de fundação, schema sincronizado e teste estrutural focado.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-BACKEND-AUDIT.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@supabase/schema.sql
@supabase/migrations/20260617080000_beat_licenses.sql
@supabase/migrations/20260620190000_secure_checkout_payments.sql
@supabase/migrations/20260621183000_purchases_member_area.sql
@src/worker.mjs
</context>

<threat_model>
## Trust Boundaries
- Checkout/Worker -> função privilegiada de banco.
- Catálogo mutável -> snapshot histórico imutável.
- Identidade privada do comprador -> contrato autorizado ao produtor.

## STRIDE Register
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| TM-01 | Tampering | `order_items` snapshot | High | Mitigate | Capturar campos completos na transação e testar schema/migration. |
| TM-02 | Information Disclosure | `buyer_identity_snapshot` | High | Mitigate | Guardar somente dados contratuais, fora de order_items visíveis ao produtor e sem metadata do provedor. |
| TM-03 | Tampering | exclusão/edição de beat, licença ou perfil | High | Mitigate | IDs e textos históricos sem FK destrutiva; perfil atual apenas como vínculo separado. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Fixar responsabilidades e invariantes da finalização</name>
  <files>.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-TRANSACTION-CONTRACT.md</files>
  <read_first>02-CONTEXT.md, 01-BACKEND-AUDIT.md, src/worker.mjs, supabase/schema.sql</read_first>
  <action>Documentar o fluxo atual e o alvo com símbolos reais. Fixar que o Worker autentica, valida provider truth e converge checkout/status/webhook; `finalize_checkout_payment()` bloqueia a tentativa e possui a transação inteira; `process_checkout()` bloqueia e valida beats/licenças e cria pedido pendente mais itens; uma função explícita de provisionamento cria um entitlement e um documento por item somente após todos os itens existirem; ledger/cupom são gravados uma vez; a tentativa é vinculada e o pedido vira `completed` apenas após cardinalidades obrigatórias. Restringir `manage_purchase_entitlements()` à revogação por reembolso ou removê-lo do caminho de criação. Incluir invariantes, ordem de rollback e tabela de ownership. Não reabrir as decisões do CONTEXT.md.</action>
  <verify>Confirmar no documento os cinco símbolos `reconcilePaymentAttempt`, `finalize_checkout_payment`, `process_checkout`, `manage_purchase_entitlements` e `manage_purchase_entitlements_trigger`, além da ordem pedido pendente -> itens -> direitos/documentos/ledger -> completed -> attempt.order_id.</verify>
  <acceptance_criteria>
    - O documento distingue claramente responsabilidades do Worker e do banco.
    - O documento proíbe criação por `completed -> completed` e commit sem cardinalidade completa.
    - O documento registra rollback integral e retorno idempotente do mesmo `order_id`.
  </acceptance_criteria>
  <done>Contrato técnico acionável e sem decisões pendentes criado.</done>
</task>

<task type="auto">
  <name>Task 2: Criar snapshot estruturado e desenhar chaves de negócio</name>
  <files>supabase/migrations/20260622210000_purchase_snapshot_foundation.sql, supabase/schema.sql</files>
  <read_first>supabase/schema.sql, supabase/migrations/20260617080000_beat_licenses.sql, supabase/migrations/20260621183000_purchases_member_area.sql, 02-CONTEXT.md</read_first>
  <action>Criar migration aditiva e idempotente. Em `orders`, adicionar `buyer_identity_snapshot jsonb` contendo somente os campos contratuais normalizados realmente fornecidos (hoje nome/e-mail e documento apenas quando existir), nunca provider payload. Em `order_items`, adicionar `beat_title_snapshot`, `beat_cover_url_snapshot`, `producer_id_snapshot` sem FK destrutiva, `producer_name_snapshot`, `license_key_snapshot`, `currency_snapshot`, `license_rights_snapshot jsonb` e `file_manifest_snapshot jsonb`. O JSON de direitos deve copiar explicitamente limites de streams/vídeos, flags unlimited, commercial/monetization/live/Content ID, crédito, duração, território, royalties e termos. Preservar as colunas legadas durante compatibilidade. Documentar `order_item_id` como chave de negócio de entitlement/documento, mas adiar índices únicos até o Plano 02 auditar e tratar duplicados legados. Sincronizar exatamente `supabase/schema.sql`.</action>
  <verify>Executar `node tests/purchase-snapshot-contract-check.js` após Task 3 e `git diff --check`.</verify>
  <acceptance_criteria>
    - A migration e o schema possuem o mesmo conjunto de campos e constraints.
    - O snapshot representa todos os campos comerciais de `beat_licenses` relevantes à licença adquirida.
    - A chave de negócio por `order_item_id` está explícita sem criar índice que falhe sobre duplicados ainda não auditados.
    - Nenhuma nova FK permite que exclusão de beat/perfil apague a identidade histórica.
  </acceptance_criteria>
  <done>Fundação de snapshot e unicidade pronta para ser preenchida pela finalização atômica.</done>
</task>

<task type="auto">
  <name>Task 3: Proteger a fundação com teste estrutural</name>
  <files>tests/purchase-snapshot-contract-check.js, package.json</files>
  <read_first>tests/checkout-database-contract-check.js, package.json, migration e schema desta tarefa</read_first>
  <action>Criar um check Node que leia migration e schema, compare os marcadores do snapshot, valide `buyer_identity_snapshot`, `license_rights_snapshot` e `file_manifest_snapshot`, exija a chave de negócio documentada por item e rejeite dependência de dados atuais para identidade histórica. Registrar `test:purchases:snapshot` no `package.json`; manter os scripts existentes intactos.</action>
  <verify>npm run test:purchases:snapshot</verify>
  <acceptance_criteria>
    - O comando falha se migration/schema divergirem ou faltar qualquer campo obrigatório.
    - O comando falha se a chave de negócio por item deixar de estar definida no contrato.
    - O comando termina com exit code 0 na implementação correta.
  </acceptance_criteria>
  <done>Contrato de snapshot verificável por comando focado.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:purchases:snapshot`
- [ ] `git diff --check`
- [ ] Somente contrato, migration, schema, teste e script previstos foram alterados.
- [ ] `#compras`, Worker, checkout, downloads e arquivos de frontend não foram modificados.
</verification>

<success_criteria>
- Contrato transacional alvo está explícito e ligado aos símbolos reais.
- Snapshot cobre DATA-01/DATA-02 sem expor dados de pagamento.
- Chave de negócio por item está definida para a migration atômica aplicar com preflight seguro.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-01-SUMMARY.md`
</output>
