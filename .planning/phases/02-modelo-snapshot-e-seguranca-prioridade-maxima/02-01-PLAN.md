---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DIAGNOSTICS.sql
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DATA-AUDIT.md
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-ROLLBACK-STRATEGY.md
autonomous: false
requirements: [DATA-03, DATA-04, DATA-05, SEC-04, PAY-02]
must_haves:
  truths:
    - "O schema real e o estado dos dados são conhecidos antes de qualquer constraint ou escrita."
    - "Pedidos incompletos, duplicidades e violações potenciais são quantificados sem alterar dados."
    - "Cada migration futura possui preflight, rollback e gate de aprovação para produção."
  artifacts:
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DIAGNOSTICS.sql"
      provides: "Queries somente leitura para schema e dados"
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DATA-AUDIT.md"
      provides: "Resultado sanitizado da inspeção"
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-ROLLBACK-STRATEGY.md"
      provides: "Sequência segura de rollout e rollback"
  key_links:
    - from: "02-DIAGNOSTICS.sql"
      to: "02-DATA-AUDIT.md"
      via: "contagens e classificações sem PII"
---

<objective>
Inspecionar schema e dados reais, identificar tudo que bloquearia as mudanças e preparar rollout/rollback sem modificar banco ou produção.

Purpose: evitar que constraints, trigger ou backfill sejam desenhados sobre suposições.
Output: queries diagnósticas, auditoria sanitizada e estratégia de rollback aprovável.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@supabase/schema.sql
@supabase/migrations/20260617080000_beat_licenses.sql
@supabase/migrations/20260620190000_secure_checkout_payments.sql
@supabase/migrations/20260620203000_secure_checkout_indexes.sql
@supabase/migrations/20260621183000_purchases_member_area.sql
@src/worker.mjs
</context>

<threat_model>
- **TM-01, High, Tampering:** diagnóstico incompleto permite constraint destrutiva. Mitigação: catálogo de objetos + contagens + amostras técnicas antes de migration.
- **TM-02, High, Information disclosure:** relatório pode vazar PII. Mitigação: apenas contagens, UUIDs mínimos e razões; nunca nomes/e-mails/tokens.
- **TM-03, High, Repudiation:** não saber o estado anterior inviabiliza rollback. Mitigação: registrar checksums, versões e queries before/after.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Inventariar schema, funções, triggers, constraints e RLS reais</name>
  <current_behavior>1. O schema consolidado contém `orders`, `order_items`, `payment_attempts`, `purchase_entitlements`, `license_documents` e ledger, mas migrations antigas e policies legadas coexistem.</current_behavior>
  <evidence>2. `schema.sql` ainda declara INSERT autenticado em orders/items; `manage_purchase_entitlements_trigger` é AFTER INSERT/UPDATE em orders; grants finais revogam `process_checkout` e limitam `finalize_checkout_payment` ao service role.</evidence>
  <action>3. Criar queries somente leitura para `pg_class`, `pg_constraint`, `pg_indexes`, `pg_trigger`, `pg_proc`, `pg_policy`, grants e versões de migration. Comparar banco real, migrations e `schema.sql`, registrar divergências e ownership exato de Worker, `process_checkout`, `finalize_checkout_payment` e trigger.</action>
  <files>4. `.planning/.../02-DIAGNOSTICS.sql`, `.planning/.../02-DATA-AUDIT.md`.</files>
  <db_objects>5. Tabelas de compras; `process_checkout`; `finalize_checkout_payment`; `manage_purchase_entitlements`; `manage_purchase_entitlements_trigger`; policies/grants associados.</db_objects>
  <dependencies>6. Acesso somente leitura ao projeto Supabase correto e Git limpo.</dependencies>
  <data_risk>7. Baixo se somente SELECT; risco de PII se resultados brutos forem copiados.</data_risk>
  <rollback>8. Nenhum rollback de dados; remover apenas artefatos diagnósticos caso contenham informação indevida e regenerar sanitizado.</rollback>
  <verify>9. Executar as queries em ambiente autorizado, comparar checksums/definições e confirmar zero DDL/DML no arquivo.</verify>
  <done>10. Auditoria lista definição real, migration de origem e divergência de cada objeto relevante, sem segredo ou PII.</done>
</task>

<task type="auto">
  <name>Task 2: Medir violações e pedidos pagos incompletos</name>
  <current_behavior>1. O trigger pode ter processado zero itens; entitlement não tem unicidade de negócio; snapshot atual é parcial.</current_behavior>
  <evidence>2. A matriz aponta pedido completed sem direito/documento, duplicação possível e ausência de currency/produtor/direitos completos em order_items.</evidence>
  <action>3. Adicionar queries de contagem e classificação para: pagamentos approved sem order; orders completed sem attempt; orders sem items; items sem entitlement/documento/ledger; múltiplos direitos/documentos por item; provider IDs repetidos; snapshots nulos/incompletos; pedidos pendentes/falhos com direito ativo; vendas exclusivas conflitantes. Não atualizar nem excluir linhas.</action>
  <files>4. `02-DIAGNOSTICS.sql`, `02-DATA-AUDIT.md`.</files>
  <db_objects>5. `payment_attempts`, `orders`, `order_items`, `purchase_entitlements`, `license_documents`, `seller_ledger_entries`, `beats`, `beat_licenses`.</db_objects>
  <dependencies>6. Task 1 e definição exata de status/provider no banco real.</dependencies>
  <data_risk>7. Baixo para SELECT; risco operacional se uma contagem for interpretada como autorização de correção automática.</data_risk>
  <rollback>8. Não aplicável a dados; preservar resultados before como baseline versionada.</rollback>
  <verify>9. Reconciliar totais por três caminhos independentes: attempt->order, order->items e item->delivery; amostrar apenas UUIDs técnicos.</verify>
  <done>10. Cada violação potencial possui quantidade, regra de classificação e lista técnica mínima para o Plano 6.</done>
</task>

<task type="auto">
  <name>Task 3: Definir rollout, reversibilidade e gates de aprovação</name>
  <current_behavior>1. Não existe sequência documentada para aplicar constraints, funções, snapshot, RLS e backfill sem risco.</current_behavior>
  <evidence>2. A Fase 2 é bloqueadora e o contexto proíbe avançar com qualquer pedido pago ambíguo ou incompleto.</evidence>
  <action>3. Escrever estratégia expand-contract: backup lógico/verificação PITR; migrations aditivas; validação em banco isolado; constraints `NOT VALID` quando aplicável; troca de função transacional; validação; dry-run de backfill; aprovação humana; aplicação; hardening; monitoramento. Definir rollback por migration e critérios de abortar, sem usar rollback destrutivo automático.</action>
  <files>4. `02-ROLLBACK-STRATEGY.md`.</files>
  <db_objects>5. Todas as migrations planejadas 20260622210000 a 20260622230000 e funções substituídas.</db_objects>
  <dependencies>6. Tasks 1-2 e confirmação de backup/PITR disponível antes de produção.</dependencies>
  <data_risk>7. Alto se rollback tentar apagar colunas ou direitos já usados; por isso rollback padrão é restaurar função/grants anteriores e preservar colunas/dados aditivos.</data_risk>
  <rollback>8. Para cada migration: down script revisado, condição de uso, perda potencial e alternativa forward-fix; nenhuma exclusão sem aprovação.</rollback>
  <verify>9. Tabletop review cobrindo falha em cada etapa e confirmação de que produção exige checkpoint explícito.</verify>
  <done>10. Documento permite aplicar ou abortar cada plano isoladamente sem perder dados históricos.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Aprovar diagnóstico antes de constraints</name>
  <current_behavior>1. Nenhuma alteração foi aplicada.</current_behavior>
  <evidence>2. `02-DATA-AUDIT.md` e `02-ROLLBACK-STRATEGY.md` concluídos.</evidence>
  <action>3. Solicitar aprovação das violações encontradas, estratégia de legado e rollback antes do Plano 2.</action>
  <files>4. Nenhum arquivo adicional.</files>
  <db_objects>5. Nenhum.</db_objects>
  <dependencies>6. Tasks 1-3.</dependencies>
  <data_risk>7. Nenhum enquanto o checkpoint bloquear execução.</data_risk>
  <rollback>8. Não aplicável.</rollback>
  <verify>9. Aprovação explícita ou revisão solicitada.</verify>
  <done>10. Usuário aprovou prosseguir ou a fase permanece parada.</done>
</task>

</tasks>

<verification>
- [ ] Git status/diff revalidado.
- [ ] Diagnóstico contém somente SELECT.
- [ ] Schema real comparado a migration e schema consolidado.
- [ ] Nenhum dado, migration, função, policy ou produção alterados.
</verification>

<success_criteria>
- Violações e ambiguidades conhecidas antes de qualquer constraint.
- Rollback e gates de produção aprováveis.
- Plano 2 não depende de suposição sobre dados legados.
</success_criteria>

<output>After completion, create `02-01-SUMMARY.md`.</output>
