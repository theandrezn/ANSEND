---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - supabase/migrations/20260622220000_purchase_delivery_backfill.sql
  - supabase/schema.sql
  - supabase/tests/purchase_delivery_backfill.sql
  - .planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md
autonomous: false
requirements: [DATA-02, DATA-04, SEC-03, SEC-04]
user_setup: []
must_haves:
  truths:
    - "Pedidos antigos comprováveis recebem exatamente os direitos e documentos ausentes sem alterar os já corretos."
    - "Pedidos ambíguos não recebem termos inventados e impedem a conclusão da fase até resolução documentada."
    - "Reexecutar o backfill após sucesso produz zero inserts e zero duplicações."
  artifacts:
    - path: "supabase/migrations/20260622220000_purchase_delivery_backfill.sql"
      provides: "Auditoria dry-run e backfill privilegiado e idempotente"
      contains: "audit_purchase_delivery_gaps"
    - path: "supabase/tests/purchase_delivery_backfill.sql"
      provides: "Regressão de casos íntegros, reparáveis e ambíguos"
    - path: ".planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md"
      provides: "Evidência antes/depois e resolução de ambiguidades"
  key_links:
    - from: "audit_purchase_delivery_gaps"
      to: "provision_purchase_delivery"
      via: "aplicação somente em order_items historicamente comprováveis"
      pattern: "p_apply"
    - from: "02-BACKFILL-REPORT.md"
      to: "purchase_entitlements, license_documents, seller_ledger_entries"
      via: "contagens antes, depois e rerun"
---

<objective>
Auditar e reparar pedidos pagos antigos de forma idempotente, bloqueando qualquer caso cuja licença histórica não possa ser comprovada.

Purpose: eliminar o passivo criado pelo trigger prematuro sem transformar dados atuais do catálogo em direitos retroativos inventados.
Output: migration de auditoria/backfill, suíte SQL e relatório auditável da execução real.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-TRANSACTION-CONTRACT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-02-SUMMARY.md
@supabase/schema.sql
</context>

<threat_model>
## Trust Boundaries
- Dados legados incompletos -> concessão de direito atual.
- Função de backfill privilegiada -> tabelas comerciais.
- Banco alvo -> relatório de auditoria versionado.

## STRIDE Register
| threat_id | category | component | severity | disposition | mitigation_plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| TM-08 | Tampering | reconstrução histórica | Critical | Mitigate | Hierarquia de evidência, dry-run e bloqueio de qualquer ambiguidade. |
| TM-09 | Elevation of Privilege | função de backfill | High | Mitigate | `security definer`, search_path explícito e execute somente service_role. |
| TM-10 | Information Disclosure | `02-BACKFILL-REPORT.md` | High | Mitigate | Somente contagens e IDs técnicos mínimos, sem PII ou provider payload. |
| TM-11 | Repudiation | aplicação do backfill | High | Mitigate | Registrar antes/depois/rerun e exigir no-op na segunda aplicação. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implementar auditoria dry-run e reparo idempotente</name>
  <files>supabase/migrations/20260622220000_purchase_delivery_backfill.sql, supabase/schema.sql</files>
  <read_first>02-CONTEXT.md, 02-TRANSACTION-CONTRACT.md, 02-02-SUMMARY.md, supabase/schema.sql</read_first>
  <action>Criar `audit_purchase_delivery_gaps(p_apply boolean default false)` como `security definer`, `search_path` explícito, sem execute para public/anon/authenticated e com grant apenas a service_role. Retornar JSON com totais: completed, sem itens, sem snapshot obrigatório, sem entitlement, sem documento, sem ledger, duplicados, reparáveis, ambíguos e inserts executados. Aplicar hierarquia explícita de evidência: primeiro snapshots existentes em order/item/documento; depois `payment_attempts.cart_items` ligado ao mesmo `order_id`; por último beat/licença/perfil atuais somente quando IDs, `created_at`/`updated_at` e triggers de timestamp provarem que a linha não mudou desde a compra. Se qualquer campo contratual não puder ser provado, classificar como ambíguo em vez de preencher por default. Pedido sem `order_items` só é reconstruível quando a tentativa vinculada prova integralmente os itens e a fonte histórica permaneceu inalterada. Em apply, preencher snapshots comprovados e chamar a rotina determinística do Plano 02 somente para reparáveis; falhar se restar ambiguidade. Depois de zero ambiguidades, tornar `order_item_id` obrigatório em entitlement/documento quando compatível com os dados reparados. Sincronizar schema.</action>
  <verify>npm run test:purchases:db</verify>
  <acceptance_criteria>
    - Dry-run não escreve dados.
    - Apply é transacional e falha antes de inventar direito ambíguo.
    - A função não fica executável por usuários autenticados.
    - Registros atuais só servem como evidência quando timestamps provam ausência de alteração posterior.
    - Registros íntegros não são reescritos e duplicados não sobrevivem.
  </acceptance_criteria>
  <done>Backfill seguro pode ser medido e aplicado sem duplicação.</done>
</task>

<task type="auto">
  <name>Task 2: Testar íntegros, reparáveis, duplicados e ambíguos</name>
  <files>supabase/tests/purchase_delivery_backfill.sql</files>
  <read_first>supabase/tests/purchase_finalization_atomicity.sql, migration desta etapa, CONTEXT.md decisões D-05 a D-09</read_first>
  <action>Adicionar pgTAP com quatro classes de fixture: pedido íntegro que permanece intocado; pedido com item/snapshot completo e direito ausente que é reparado; pedido com duplicidade legado que converge para uma linha canônica; pedido sem evidência suficiente que aparece como ambíguo e impede apply. Executar dry-run, apply e segundo apply, comparando contagens e conteúdo. Verificar que alterações posteriores no beat/licença não afetam o contrato reconstruído de snapshot completo.</action>
  <verify>npm run test:purchases:db</verify>
  <acceptance_criteria>
    - Segundo apply retorna zero inserts/updates materiais.
    - Caso ambíguo nunca recebe entitlement ou documento.
    - Caso reparável termina com cardinalidade exata por item.
    - Teste falha se a função buscar termos atuais para completar snapshot ausente.
  </acceptance_criteria>
  <done>Backfill provado como conservador, repetível e auditável.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Aplicar migrations da Fase 2 no alvo autorizado</name>
  <files>supabase/migrations/20260622210000_purchase_snapshot_foundation.sql, supabase/migrations/20260622213000_atomic_purchase_finalization.sql, supabase/migrations/20260622220000_purchase_delivery_backfill.sql</files>
  <read_first>02-01-SUMMARY.md, 02-02-SUMMARY.md, resultados de `npm run test:purchases:snapshot`, `npm run test:purchases:finalization-contract` e `npm run test:purchases:db`, supabase/config.toml</read_first>
  <action>Depois que snapshot, finalização e backfill passarem no banco isolado, executar `supabase db push --dry-run` no projeto autorizado e conferir que somente as migrations 20260622210000, 20260622213000 e 20260622220000 serão aplicadas. Confirmar o project ref `qxujynzqdursxaehchik`, executar `supabase db push` de forma não interativa com credencial segura e interromper em qualquer erro. Não executar backfill apply nesta tarefa; apenas instalar schema/funções para permitir o dry-run factual seguinte.</action>
  <verify>Executar `supabase migration list` e confirmar as três versões presentes local e remotamente; reabrir as definições das funções no alvo.</verify>
  <acceptance_criteria>
    - Dry-run foi revisado antes do push.
    - Apenas migrations já aprovadas desta fase foram aplicadas.
    - Push falho interrompe o plano e não é tratado como sucesso.
    - A função de backfill ainda não foi chamada com `p_apply = true`.
  </acceptance_criteria>
  <done>Schema e funções 01-03 estão no alvo, prontos para auditoria dry-run.</done>
</task>

<task type="auto">
  <name>Task 4: Executar dry-run no alvo e registrar evidência</name>
  <files>.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md</files>
  <read_first>migration desta etapa, configuração Supabase atual, regras de não registrar dados sensíveis</read_first>
  <action>Executar primeiro `p_apply = false` no banco alvo autorizado. Registrar no relatório somente contagens, IDs técnicos mínimos necessários para investigação e classificação, sem nomes, e-mails, documentos, tokens ou payloads de pagamento. Se houver ambiguidades, parar antes de apply e listar a evidência faltante de cada caso. Se não houver, registrar explicitamente zero ambiguidades.</action>
  <verify>Revisar o relatório e confirmar que total de ambiguidades e decisão de apply estão explícitos, sem PII.</verify>
  <acceptance_criteria>
    - Relatório contém timestamp, ambiente, migration, contagens antes e resultado do dry-run.
    - Nenhum dado pessoal ou segredo aparece no documento.
    - Apply não foi executado quando ambiguidades são maiores que zero.
  </acceptance_criteria>
  <done>Estado legado real medido e pronto para gate factual.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Checkpoint: Resolver ambiguidades históricas sem presumir direitos</name>
  <files>.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md</files>
  <decision>Como prosseguir caso o dry-run encontre pedidos ambíguos?</decision>
  <context>A política já está decidida: ambiguidades bloqueiam a fase e nenhum direito pode ser inventado. Este checkpoint escolhe apenas entre resolver cada caso com nova evidência auditável ou abortar o backfill até essa evidência existir.</context>
  <options>
    - `resolve-with-evidence`: localizar comprovantes históricos, atualizar apenas o snapshot comprovado e repetir o dry-run.
    - `abort-backfill`: não aplicar o backfill; manter as Fases 2 e 3 bloqueadas até haver evidência.
  </options>
  <action>Se houver ambiguidades, pausar e solicitar a escolha factual acima. Não executar apply até cada caso possuir evidência histórica suficiente. Se o dry-run retornar zero ambiguidades, registrar isso e prosseguir.</action>
  <verify>Confirmar no relatório que `ambiguous_count = 0` antes da Task 4.</verify>
  <resume-signal>Selecione resolve-with-evidence ou abort-backfill. Se o dry-run retornar zero ambiguidades, este checkpoint é auto-resolvido como resolve-with-evidence.</resume-signal>
  <done>Todos os casos ambíguos foram resolvidos por evidência ou a execução foi interrompida mantendo a fase bloqueada.</done>
</task>

<task type="auto">
  <name>Task 5: Aplicar, repetir e fechar o relatório</name>
  <files>.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-BACKFILL-REPORT.md</files>
  <read_first>relatório dry-run aprovado e evidências resolvidas</read_first>
  <action>Somente com zero ambiguidades, executar apply em transação, repetir dry-run, executar apply uma segunda vez e registrar contagens finais. Exigir zero pedidos completed sem itens/direitos/documentos/ledger, zero duplicados e zero mudanças materiais no segundo apply. Se qualquer invariante falhar, registrar erro e deixar a fase bloqueada.</action>
  <verify>Comparar relatório com consultas de cardinalidade e confirmar rerun no-op.</verify>
  <acceptance_criteria>
    - Zero completed orders incompletos no escopo auditado.
    - Zero ambiguidades e duplicidades remanescentes.
    - Segundo apply não cria nem altera direitos/documentos/ledger.
    - Relatório registra antes, primeiro apply, depois e rerun.
  </acceptance_criteria>
  <done>Passivo histórico reparado ou fase explicitamente bloqueada, nunca silenciosamente ignorada.</done>
</task>

</tasks>

<verification>
- [ ] `npm run test:purchases:db`
- [ ] Dry-run no alvo foi registrado antes do apply.
- [ ] Zero ambiguidades antes do apply.
- [ ] Rerun do backfill é no-op.
- [ ] Relatório não contém PII, tokens ou provider payload.
</verification>

<success_criteria>
- Todo pedido legado completed está íntegro ou a fase permanece bloqueada.
- Nenhum direito foi inferido do catálogo atual sem snapshot histórico suficiente.
- Backfill é repetível, observável e protegido por service role.
</success_criteria>

<output>
After completion, create `.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-03-SUMMARY.md`
</output>
