---
phase: 02-modelo-snapshot-e-seguranca-prioridade-maxima
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - supabase/migrations/20260622213000_atomic_purchase_finalization.sql
  - supabase/schema.sql
  - src/worker.mjs
  - tests/purchase-finalization-contract-check.js
autonomous: true
requirements: [DATA-03, DATA-05, DATA-06, PAY-02, PAY-06]
must_haves:
  truths:
    - "Direitos nunca são criados antes de todos os order_items existirem."
    - "Falha intermediária reverte pedido, itens, direito, contrato, ledger, cupom e exclusividade."
    - "completed -> completed não participa do provisionamento."
  artifacts:
    - path: "supabase/migrations/20260622213000_atomic_purchase_finalization.sql"
      provides: "Ordem transacional e responsabilidade explícita"
    - path: "tests/purchase-finalization-contract-check.js"
      provides: "Guarda contra retorno do trigger prematuro"
  key_links:
    - from: "src/worker.mjs"
      to: "finalize_checkout_payment"
      via: "checkout, status e webhook convergem no mesmo attempt"
    - from: "finalize_checkout_payment"
      to: "process_checkout/provision_purchase_delivery"
      via: "uma única transação"
---

<objective>
Corrigir a ordem transacional do pedido e definir uma única autoridade de finalização idempotente.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-CONTEXT.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-DISCUSSION-LOG.md
@.planning/phases/01-auditoria-da-implementacao-atual/01-CHANGE-MATRIX.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modelo-snapshot-e-seguranca-prioridade-maxima/02-02-SUMMARY.md
@supabase/schema.sql
@src/worker.mjs
</context>

<threat_model>
- **TM-07, Critical, Partial commit:** pedido pago sem entrega. Mitigação: uma transaction e asserts de cardinalidade.
- **TM-08, Critical, Race:** webhooks concorrentes finalizam duas vezes. Mitigação: attempt `FOR UPDATE` + unique keys.
- **TM-09, High, Elevation:** cliente chama helper interno. Mitigação: revoke e service role apenas no finalizador.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Substituir o trigger prematuro por provisionamento explícito</name>
  <current_behavior>1. `process_checkout` insere order já completed; o AFTER INSERT em orders roda antes dos items e processa zero linhas.</current_behavior>
  <evidence>2. `manage_purchase_entitlements_trigger` chama `manage_purchase_entitlements`; depois `finalize_checkout_payment` faz completed -> completed, condição que não recria direitos.</evidence>
  <action>3. Remover o trigger atual do caminho de criação. Criar `provision_purchase_delivery(p_order_id)` interna e determinística, chamada explicitamente após todos os items. Se necessário, substituir o trigger de reembolso por `revoke_purchase_entitlements_trigger` separado, somente completed -> refunded.</action>
  <files>4. Migration 20260622213000, `schema.sql`, teste de contrato.</files>
  <db_objects>5. `manage_purchase_entitlements_trigger`, `manage_purchase_entitlements`, novo helper `provision_purchase_delivery`, trigger/função de revogação.</db_objects>
  <dependencies>6. Delivery keys do Plano 2.</dependencies>
  <data_risk>7. Alto: remover trigger sem ligar helper interromperia entrega; migration deve substituir função e trigger atomicamente.</data_risk>
  <rollback>8. Restaurar definição anterior apenas se escrita de novos pedidos estiver pausada; preferir forward-fix e preservar direitos já criados.</rollback>
  <verify>9. Teste exige ausência de criação no trigger e presença da chamada explícita depois da inserção dos items.</verify>
  <done>10. Pedido sem order_items não gera entitlement e completed -> completed é irrelevante.</done>
</task>

<task type="auto">
  <name>Task 2: Reordenar process_checkout e finalize_checkout_payment</name>
  <current_behavior>1. `process_checkout` cria completed, items e exclusividade; finalizador aplica metadata/ledger e vincula attempt depois.</current_behavior>
  <evidence>2. A função atual pode completar sem direito porque a ausência de items no trigger não lança erro.</evidence>
  <action>3. Fazer `process_checkout` bloquear beats em ordem determinística, validar licenças e criar order pending + todos os items. Em `finalize_checkout_payment`: bloquear attempt approved, retornar order existente em replay, aplicar confirmação/metadados seguros do pagamento, criar delivery e contrato, ledger/cupom, verificar cardinalidades, marcar order completed e vincular attempt.order_id por último. Download só será liberável após commit completed + entitlement active.</action>
  <files>4. Migration 20260622213000 e `schema.sql`.</files>
  <db_objects>5. `process_checkout`, `finalize_checkout_payment`, orders/items/payment_attempts/entitlements/documents/ledger/coupons/beats/licenses.</db_objects>
  <dependencies>6. Task 1 e constraints do Plano 2.</dependencies>
  <data_risk>7. Crítico: ordem incorreta pode deixar venda exclusiva ou cupom parcial.</data_risk>
  <rollback>8. Toda falha usa exception para rollback automático; rollback de release restaura versão anterior somente com checkout pausado.</rollback>
  <verify>9. Testes SQL do Plano 7 injetam falha em item, delivery e ledger; nenhuma tabela deve persistir efeito parcial.</verify>
  <done>10. Estado completed só é atingido com items, entitlement, documento e ledger exatos.</done>
</task>

<task type="auto">
  <name>Task 3: Unificar replay do Worker e tratamento de erro</name>
  <current_behavior>1. Checkout, status e webhook convergem em `reconcilePaymentAttempt`, que chama o RPC em approved.</current_behavior>
  <evidence>2. O Worker já usa `finalizeApprovedAttempt`, mas testes não provam reenvio completo nem erro atômico.</evidence>
  <action>3. Preservar rotas/HMAC/checkout. Ajustar apenas propagação de resultado/erro necessária para que os três caminhos usem o mesmo attempt e nunca respondam paid quando o RPC falhar. Exportar helper interno somente se necessário aos testes, sem endpoint novo.</action>
  <files>4. `src/worker.mjs`, `tests/purchase-finalization-contract-check.js`.</files>
  <db_objects>5. RPC `finalize_checkout_payment`; nenhuma service key nova.</db_objects>
  <dependencies>6. Task 2.</dependencies>
  <data_risk>7. Médio: regressão de resposta do checkout; manter shape público atual.</data_risk>
  <rollback>8. Reverter somente adaptação do Worker; RPC permanece idempotente.</rollback>
  <verify>9. Simular checkout/status/webhook approved repetidos e erro RPC; todos usam o mesmo p_attempt_id.</verify>
  <done>10. Webhooks concorrentes são serializados pelo banco e falha de finalização nunca é sucesso aparente.</done>
</task>

</tasks>

<verification>
- [ ] Trigger prematuro removido/substituído explicitamente.
- [ ] Ordem obrigatória documentada e implementável.
- [ ] Rollback atômico para cada falha intermediária.
- [ ] Nenhuma dependência de completed -> completed.
</verification>

<success_criteria>
- Uma única autoridade transacional.
- Replay retorna o mesmo pedido.
- Nenhum pedido completed parcial é possível.
</success_criteria>

<output>After completion, create `02-03-SUMMARY.md`.</output>
