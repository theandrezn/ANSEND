{
  "name": "ansend-platform",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build-worker.js",
    "deploy": "npm run build && wrangler deploy",
    "test:routes": "node tests/route-stability-check.js",
    "test:auth-focus": "node tests/auth-focus-check.js"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "wrangler": "^4.98.0"
  }
}
